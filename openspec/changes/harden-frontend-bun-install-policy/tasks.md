# Tasks

- [x] Update `Dockerfile` so frontend policy files are copied before `bun install`.
- [x] Update `Dockerfile.distroless` with the same frontend policy-file ordering.
- [x] Update the Compose frontend inline Dockerfile with context-relative policy copies before `bun install`.
- [x] Verify install ordering and the final uncommitted diff.
